<!DOCTYPE html>
<html lang="fr">
<head>
    <title>Profil de l'Utilisateur</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 20px;
        }

        h1 {
            color: #333;
        }

        h2 {
            color: #555;
        }

        p {
            margin: 10px 0;
        }

        ul {
            list-style-type: none;
            padding: 0;
        }

        li {
            border: 1px solid #ddd;
            padding: 10px;
            margin-bottom: 10px;
            border-radius: 5px;
        }
    </style>
</head>
<body>
<h1>Profil de {{ utilisateur.nom }} {{ utilisateur.prenom }}</h1>
<p>Adresse Email : {{ utilisateur.email }}</p>
<h2>Touites de {{ utilisateur.prenom }}</h2>
<ul>
    {% for touite in touites %}
        <li>{{ touite.contenu }}</li>
    {% endfor %}
</ul>
</body>
</html>
